import { GenericPipePipe } from './generic-pipe.pipe';

describe('GenericPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GenericPipePipe();
    expect(pipe).toBeTruthy();
  });
});
